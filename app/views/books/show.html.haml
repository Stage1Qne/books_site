.book.cf
  .ui.grid
    .seven.wide.column.left
      .cover
        .ui.blue.segment
          = image_tag @book.cover.url(:medium)
      .author
        .ui.blue.message
          %h2{align: "center"} AUTHOR
          .author_avatar
            = image_tag @book.author.avatar.url(:medium), class: "ui circular image"
          %h2#author_name= @book.author.full_name
    .eight.wide.column.right
      .inf
        .ui.piled.blue.segment
          .ui.ribbon.teal.label
            .ui.huge.header= @book.name
          .table_params
            %table.ui.basic.table
              %tbody
                %br
                %tr
                  %td{align: "right"}
                    Series
                    %i.bookmark.icon
                  %td= @book.series
                %tr
                  %td{align: "right"}
                    Publisher
                    %i.users.icon
                  %td= @book.publisher
                %tr
                  %td{align: "right"}
                    Publishing year
                    %i.calendar.icon
                  %td= @book.year
                %tr
                  %td{align: "right"}
                    Pages
                    %i.barcode.icon
                  %td= @book.pages
                %tr
                  %td{align: "right"}
                    Binding
                    %i.book.icon
                  %td= @book.binding
                %tr
                  %td{align: "right"}
                    Format
                    %i.file.icon
                  %td= @book.format
                %tr
                  %td{align: "right"}
                    ISBN
                    %i.globe.icon
                  %td= @book.isbn
                %tr
                  %td{align: "right"}
                    Weight
                    %i.dashboard.icon
                  %td= @book.weight

          %h4.ui.top.purple.attached.header Description
          .ui.purple.segment.attached
            %p= @book.decription
.author-books
  .ui.five.connected.items
    .row
      - @author_books.each do |b|
        -if not b==@book
          %a.item{href: b.id}
            .content
              .name= b.name
              -s = b.decription.split(" ").each_with_object("") {|x,ob| break ob unless (ob.length + " ".length + x.length <= 200);ob << (" " + x)}.strip
              %p.description= s << "..."

.comments
  .ui.info.message
    .header
      %h3{align: "center"}
        %i.icon.inverted.circular.teal.comment
        COMMENTS
  .ui.threaded.comments
    =render 'comments/comments'

  .new-comment
    = render 'comments/new_comment'
